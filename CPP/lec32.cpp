#include "bits/stdc++.h"
using namespace std;

/* void toUpperCase (string &s) {
    for (int i=0; i<s.length(); i++) {
        if (s[i] >= 'a' && s[i] <= 'z')
            s[i] -= 32;
    }
}

void toLowerCase (string &s) {
    for (int i=0; i<s.length(); i++) {
        if (s[i] >= 'A' && s[i] <= 'Z')
            s[i] += 32;
    }
} */

int main () {
    string s;
    getline(cin, s);
    cout << s << endl;
}